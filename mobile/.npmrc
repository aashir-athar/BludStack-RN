# Expo/React Native projects routinely hit npm peer-resolution conflicts from
# peerOptional deps (e.g. @react-native-community/datetimepicker -> react-native-windows).
# legacy-peer-deps lets the SDK-pinned versions install cleanly and keeps local
# + CI installs identical. Expo manages compatibility via `expo install`.
legacy-peer-deps=true
